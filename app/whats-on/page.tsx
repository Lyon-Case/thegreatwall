'use client'
import { useMemo, useState } from 'react'
import { CalendarDays, Filter, Ticket } from 'lucide-react'
import { FuturisticButton, PageHero, PageShell, SectionLabel } from '@/components/site-shell'
const events = [{ date: '14 SEP', category: 'MUSIC', title: 'Sunday Sundowners', copy: 'Live sets, sunset plates and cocktails in the courtyard.', tone: 'cyan' }, { date: '20 SEP', category: 'FOOD + CULTURE', title: 'Botswana on a Plate', copy: 'An evening of local stories, fire and flavour.', tone: 'lime' }, { date: '04 OCT', category: 'NIGHTLIFE', title: 'The Great Wall After Dark', copy: 'DJs, dancing and late-night bites until the signal fades.', tone: 'violet' }, { date: '18 OCT', category: 'COMMUNITY', title: 'Open Wall Sessions', copy: 'Creative talks, emerging artists and a room full of ideas.', tone: 'cyan' }]
export default function WhatsOnPage() { const [filter, setFilter] = useState('ALL'); const filtered = useMemo(() => filter === 'ALL' ? events : events.filter((e) => e.category === filter), [filter]); return <PageShell><PageHero eyebrow="WHAT'S ON / LIVE FEED" title={<>Make a date<br /><em>with the wall.</em></>} copy="Your live feed for music, food, culture and the nights that refuse to be ordinary." /><section className="content-section"><div className="section-heading-row"><div><SectionLabel>Event index / September—October 2026</SectionLabel><h2>Upcoming <em>signals.</em></h2></div><FuturisticButton href="/tickets">View ticketing</FuturisticButton></div><div className="filter-row futuristic-filter">{['ALL', 'MUSIC', 'FOOD + CULTURE', 'NIGHTLIFE', 'COMMUNITY'].map((item) => <button key={item} className={filter === item ? 'filter active' : 'filter'} onClick={() => setFilter(item)}><Filter size={13} /> {item}</button>)}</div><div className="event-feed">{filtered.map((event, index) => <article className={`feed-card ${event.tone}`} key={event.title}><div className="feed-date"><CalendarDays size={16} /><strong>{event.date}</strong><span>2026 / 0{index + 1}</span></div><div className="feed-body"><span className="event-type">{event.category}</span><h3>{event.title}</h3><p>{event.copy}</p><div><span className="venue-chip">THE GREAT WALL · GABORONE</span><a href="/tickets" className="event-link"><Ticket size={15} /> Get tickets ↗</a></div></div></article>)}</div></section></PageShell> }
